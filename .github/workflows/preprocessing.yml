name: Data Preprocessing Pipeline

on:
  push:
    paths:
      - 'league_of_legends_raw/**'
      - 'preprocessing/automate_fikriars.py'
  workflow_dispatch:

jobs:
  preprocess:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12.7'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas scikit-learn

    - name: Run automation script
      run: |
        cd preprocessing
        python automate_fikriars.py --input ../league_of_legends_raw/high_diamond_ranked_10min.csv --output_dir ../league_of_legends_preprocessing

    - name: Commit and push changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add league_of_legends_preprocessing/data_clean.csv
        git commit -m "Auto-update preprocessed dataset" || echo "No changes to commit"
        git push
